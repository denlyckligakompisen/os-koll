name: Daily FIFA Schedule Update
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: npm run scrape:fifa
      - run: npm run scrape:fifa-standings
      - run: node scripts/scrape_allsvenskan.js
      - run: node scripts/scrape_sirius.js
      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add public/data/worldcup_2026_matches.json public/data/worldcup_2026_groups.json public/data/allsvenskan_matches.json public/data/allsvenskan_standings.json public/data/sirius_matches.json public/data/sirius_standings.json
          git commit -m "Update schedule data [skip ci]" || exit 0
          git push
